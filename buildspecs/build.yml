version: 0.2

phases:
  install:
    runtime-versions:
      java: "$JAVA_RUNTIME"

  build:
    commands:
      - mvn clean install -pl :bom-internal,:codegen-generated-classes-test -am -Pquick
      - while mvn surefire:test -pl :codegen-generated-classes-test -Dtest=AsyncStreamingCoreMetricsTest#apiCall_allRetryAttemptsFailedOfNetworkError; do :; done